<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

  <display-name>UTESHOP</display-name>

  <!-- Listener stub -->
  <listener>
    <listener-class>vn.ute.uteshop.listeners.AppContextListener</listener-class>
  </listener>

  <!-- "SiteMesh" placeholder filter (custom, not real sitemesh lib) -->
  <filter>
    <filter-name>SiteMeshFilter</filter-name>
    <filter-class>vn.ute.uteshop.filters.SiteMeshFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>SiteMeshFilter</filter-name>
    <url-pattern>/*</url-pattern>
    <dispatcher>REQUEST</dispatcher>
    <dispatcher>FORWARD</dispatcher>
  </filter-mapping>

  <welcome-file-list>
    <welcome-file>index.jsp</welcome-file>
  </welcome-file-list>
</web-app>
